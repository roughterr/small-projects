package com.roughterr;

/**
 * Message manager (a multi threading implementation).
 */
public class MessageManagerMultiThread implements MessageManager {
    @Override
    public void sendMessage(Message message) {

    }
}
